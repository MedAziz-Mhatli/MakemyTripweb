<form method="post" action="{{ path('delete',{'id':l.idHotel}) }}" onsubmit="return confirm('Are you sure you want to delete this blog?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete', ~ l.idHotel}) }}">
    <button class="btn">Delete</button>
</form>