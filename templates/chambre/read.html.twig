{% extends 'backBase.html.twig' %}

{% block title %}Hello Admin!{% endblock %}

{% block body %}

    <div class="main-panel">
        <div class="content-wrapper">
            <div class="page-header">
                <h3 class="page-title"> Chambres </h3>

            </div>
            <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">

                        <div class="table-responsive">
                            <table class="table table-striped ">
                                <thead>
                                <tr class="hh">
                                    <th> # </th>
                                    <th> Type </th>
                                    <th> Vue </th>
                                    <th> Prix </th>


                                </tr>
                                </thead>
                                {% for j in tabb %}
                                    <tbody>
                                    <!-- ***** Yosra Note **** -->
                                    <!-- Bech t7il for lina t3abi el table -->
                                    <tr onclick="" class="hh">
                                        <td class="py-1">
                                            <!-- ***** Yosra Note **** -->
                                            <!-- ***** assert('Products/'~product.id) -->
                                            <img src="{{ asset('back/images/faces-clipart/pic-1.png') }}" alt="image" />
                                        </td>
                                        <th> {{ j.type }} </th>
                                        <td> {{ j.vue }} </td>
                                        <td> {{ j.prixNuitee }} </td>
                                        <td> <a class="" href="{{ path('updatech',{'id':j.idChambre}) }}">
                                                <i class="mdi mdi-pencil"></i>
                                            </a></td>

                                        <td> <a class="" href="{{ path('deletee',{'id':j.idChambre}) }}">
                                                <i class="mdi mdi-delete-forever"></i>
                                            </a></td>


                                    </tr>

                                    </tbody>
                                {% endfor %}
                            </table>

                        </div>

                </div>
            </div>
            <ul class="nav">

                <li class="nav-item menu-items">
                    <a class="nav-link" href="{{ asset('/createC') }}">
              <span class="menu-icon">
                <i class="mdi mdi-speedometer"></i>
              </span>
                        <span class="menu-title">Ajouter Chambre</span>
                    </a>
                </li>





            </ul>
        </div>
        <ul>
            <li>
                <a class="" href="{{ path('chambre',{'id':81}) }}">
                    hhh
                </a>
            </li>
        </ul>
    </div>
{% endblock %}
