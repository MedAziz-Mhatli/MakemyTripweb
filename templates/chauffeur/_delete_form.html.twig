<form method="post" action="{{ path('app_chauffeur_delete', {'id': chauffeur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chauffeur.id) }}">
    <button class="btn">Delete</button>
</form>
