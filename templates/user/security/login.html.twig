{% extends 'base.html.twig' %}

{% block content %}

    <div class="container">

    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">{{ 'Connexion'|trans }}</div>
                <div class="card-body">
                {% if error %}
                    <div class="alert alert-danger">
                        {{ error.messageKey|trans(error.messageData, 'security') }}
                    </div>
                {% endif %}

                {{ form_start(form) }}
                {{ form_row(form._username) }}
                {{ form_row(form._password) }}
                {{ form_row(form.remember_me) }}
                <button type="submit" class="btn btn-default btn-primary btn-block">{{ 'Se connecter'|trans }}
                    <span class="fa fa-lock"></span>
                </button>
                <br>
                <a href="{{ path('user_register') }}">{{ 'Vous n avez pas encore de compte ?'|trans }}</a> |
                <a href="{{ path('user_request_password_reset') }}">{{ 'Mot de passe oublié !'|trans }}</a>
                {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}