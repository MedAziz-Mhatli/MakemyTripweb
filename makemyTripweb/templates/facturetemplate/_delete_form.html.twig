<form method="post" action="{{ path('facturetemplate_delete', {'idFacture': facture.idFacture}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture.idFacture) }}">
    <button class="btn btn-primary" >Delete🗑️</button>
</form>
