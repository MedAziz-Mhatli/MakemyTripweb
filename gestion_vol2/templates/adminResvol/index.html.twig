{% extends "backBase.html.twig" %}

{% block body %}

    <div class="main-panel">
        <div class="content-wrapper">
                    <ul class="navbar-nav w-100">
                <li class="nav-item w-100">
            
                    
                    </form>
                </li>
            </ul>
            <ul class="navbar-nav w-100">
                <li class="nav-item w-100">
                    <form class="nav-link mt-2 mt-md-0 d-none d-lg-flex search" method="post" action="{{ path('search') }}">
                        <input type="text" name = "search"  class="form-control" placeholder="Search company">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><input class="btn btn-success" type="submit"  value="search"></li>
                        </ol>
                    </form>
                </li>
            </ul>
            <div class="page-header">
                <h3 class="page-title"> reservation vol : </h3>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                       
                    </ol>
                </nav>
            </div>
            
                <div class="col-lg-12 grid-margin stretch-card">
                    <div class="card">
                        <div class="card-body">
                            <div class="table-responsive">
                                <table class="table table-striped ">
                                    <thead>
                                    <tr>
                                        <th> Picture </th>
                                        <th> ID </th>
                                        <th> date vol </th>
                                        <th> compagne </th>
                                        <th> class  </th>
                                        <th> nbr personne </th>
                                      
                                    </tr>
                                    </thead>
                                    <tbody>
                                 {% for ResVol in ResVols %}
                                    <tr onclick="">
                                        <td class="py-1">
                                        
                                            <img src="{{ asset('back/images/faces-clipart/pic-1.png') }}" alt="image" />
                                        </td>
                                         <td> {{ResVol.getIdResVol}} </td>
                                        <th> {{ResVol.getDateResVol|date('d-m-Y')}} </th>
                                        <td> {{ResVol.getCompagnieAerienne}} </td>
                                        <td> {{ResVol.getClasse}} </td>
                                        <td> {{ResVol.getNbPersonnes}} </td> 

                                                       

                                            <td> <a class="" href="{{ path('admin_resVol_delete',{'id':ResVol.getIdResVol})}}">
                                                <i class="mdi mdi-delete-forever"></i>
                                            </a></td>
                                    </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

    </div>
{% endblock %}